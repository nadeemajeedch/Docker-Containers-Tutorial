# Submission notes (student)

Replace this file with your own write-up before handing in.

- Image builds from `Dockerfile`
- Jupyter via `docker compose up`
- Notebooks persist under `notebooks/`
- Data under `data/`
- Figure written to `results/`
- Classmate reproduces with `docker compose up --build`
